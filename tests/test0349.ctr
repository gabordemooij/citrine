☞ x ≔ Nil.
✎ write: x code object type, stop.
✎ write: x code object string, stop.

☞ x ≔ True.
✎ write: x code object type, stop.
✎ write: x code object string, stop.

☞ x ≔ False.
✎ write: x code object type, stop.
✎ write: x code object string, stop.

☞ x ≔ 1.
✎ write: x code object type, stop.
✎ write: x code object string, stop.

☞ x ≔ -3.
✎ write: x code object type, stop.
✎ write: x code object string, stop.

☞ x ≔ 9.2.
✎ write: x code object type, stop.
✎ write: x code object string, stop.

☞ x ≔ -9.2.
✎ write: x code object type, stop.
✎ write: x code object string, stop.

☞ x ≔ List new.
✎ write: x code object type, stop.
✎ write: x code object string, stop.

☞ x ≔ List ← 1 ; 2.
✎ write: x code object type, stop.
✎ write: x code object string, stop.

☞ x ≔ List ← 1 ; (List ← 2).
✎ write: x code object type, stop.
✎ write: x code object string, stop.

☞ x ≔ List ← 1 ; (List ← 2) ; 3.
✎ write: x code object type, stop.
✎ write: x code object string, stop.

☞ x ≔ List ← 1 ; ((Map new) put:‘a’ at:‘b’) ; 3.
✎ write: x code object type, stop.
✎ write: x code object string, stop.

☞ x ≔ List ← 1 ; ((Map new) put:(List ← 9) at:‘b’) ; 3.
✎ write: x code object type, stop.
✎ write: x code object string, stop.

☞ x ≔ (Map new) put:2 at:‘1’.
✎ write: x code object type, stop.
✎ write: x code object string, stop.

☞ x ≔ (Map new) put:2 at:‘x’.
✎ write: x code object type, stop.
✎ write: x code object string, stop.

☞ x ≔ (Map new) put:(List ← 1 ; 2) at:‘x’.
✎ write: x code object type, stop.
✎ write: x code object string, stop.

☞ x ≔ (Map new) put:(List ← ((Map new) put:‘q’ at:‘y’) ; 2) at:‘x’.
✎ write: x code object type, stop.
✎ write: x code object string, stop.

✎ write: ‘2 + 3’ object, stop.
✎ write: ‘2 + 3’ object type, stop.




