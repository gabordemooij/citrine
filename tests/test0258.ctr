☞ myFile := File new: '/tmp/lock'.

myFile exists true: {
	myFile delete.
}.

myFile write: 'lock'.

☞ isLocked   := False.
☞ isUnlocked := False.

{ isLocked := myFile lock. ↲ isLocked. } false: { Moment wait: 1. }.
✎ write: isLocked, end.

{ isLocked := myFile lock. ↲ isLocked. } false: { Moment wait: 1. }.
✎ write: isLocked, end.

☞ myFile2 := File new: '/tmp/lock2'.

myFile2 exists true: {
	myFile2 delete.
}.

myFile2 write: 'lock'.

{ isLocked := myFile2 lock. ↲ isLocked. } false: { Moment wait: 1. }.
✎ write: isLocked, end.

{ isUnlocked := myFile2 unlock. ↲ isUnlocked. } false: { Moment wait: 1. }.
✎ write: isUnlocked, end.
