

Number on: ‘digits:’ do: { :number
	☞ string ≔ ⛏ string.
	☞ digits ≔ List new.
	☞ diff ≔ (number - string length).
	digits fill: diff with: ‘0’.
	☞ fixed ≔ digits join: ‘’.
	fixed append: string.
	↲ fixed.
}.