'SINGLE_LANGUAGE'.


☞ usedMemory1 := 0.
☞ usedMemory2 := 0.
☞ usedMemory3 := 0.
☞ usedMemory4 := 0.

Program tidiness: 0.

☞ a := List ← 'hello' ; 'world' ; True ; Nil ; 666.
a := a + ( List ← 1 ).
✎ write: ( 'sum: ' + ( a sum ) ), brk.
✎ write: ( 'min: ' + ( a min ) ), brk.
✎ write: ( 'max: ' + ( a max ) ), brk.
✎ write: ( '3rd: ' + ( a @ 2 ) ), brk.
✎ write: ( 'prd: ' + ( a product ) ), brk.
a sort: { :a :b ↲ (b - a). }.
a each: { :i :e ✎ write: ( i ), brk. }.

Program sweep.
usedMemory1 := (Program memory @ 0).


a := List ← 'hello' ; 'world' ; True ; Nil ; 666.
a := a + ( List ← 1 ).
✎ write: ( 'sum: ' + ( a sum ) ), brk.
✎ write: ( 'min: ' + ( a min ) ), brk.
✎ write: ( 'max: ' + ( a max ) ), brk.
✎ write: ( '3rd: ' + ( a @ 2 ) ), brk.
✎ write: ( 'prd: ' + ( a product ) ), brk.
a sort: { :a :b ↲ (b - a). }.
a each: { :i :e ✎ write: ( i ), brk. }.


Program sweep.
usedMemory2 := (Program memory @ 0).


a := List ← 'hello' ; 'world' ; True ; Nil ; 666.
a := a + ( List ← 1 ).
✎ write: ( 'sum: ' + ( a sum ) ), brk.
✎ write: ( 'min: ' + ( a min ) ), brk.
✎ write: ( 'max: ' + ( a max ) ), brk.
✎ write: ( '3rd: ' + ( a @ 2 ) ), brk.
✎ write: ( 'prd: ' + ( a product ) ), brk.
a sort: { :a :b ↲ (b - a). }.
a each: { :i :e ✎ write: ( i ), brk. }.


Program sweep.
usedMemory3 := (Program memory @ 0).



( usedMemory1 = usedMemory2 and: usedMemory1 = usedMemory3, and: usedMemory2 = usedMemory3 ) true: {
	✎ write: 'No memory leaks here'.
}, false: {
	✎ write: 'Leaking memory...'.
}.
